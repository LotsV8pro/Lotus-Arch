# Auto-generated by Arctis Sound Manager — passthrough (all gains = 0)
# ASM-CONF-VERSION: 4
context.modules = [
  { name = libpipewire-module-filter-chain
    flags = [ nofail ]
    args = {
      node.description = "Sonar EQ (bypass)"
      filter.graph = {
        nodes = [
                    { type = builtin  name = copy  label = copy }
        ]
      }
      capture.props = {
        node.name         = "effect_input.sonar-aux-eq"
        media.class       = Audio/Sink/Internal
        priority.session  = 1000
        audio.channels = 8
        audio.position = [ FL FR FC LFE RL RR SL SR ]
      }
      playback.props = {
        node.name           = "effect_output.sonar-aux-eq"
        node.target         = "effect_input.virtual-surround-7.1-hesuvi"
        target.object       = "effect_input.virtual-surround-7.1-hesuvi"
        node.autoconnect     = false
        state.restore-target = false
        node.dont-fallback  = true
        node.linger         = true
        node.passive        = true
        audio.channels      = 8
        audio.position      = [ FL FR FC LFE RL RR SL SR ]
      }
    }
  }
]
